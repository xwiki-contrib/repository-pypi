package org.xwiki.contrib.repository.pypi.searching;

import java.io.InputStream;
import java.util.List;

import org.apache.lucene.document.Document;
import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Created by Krzysztof on 24.07.2017.
 */
public class PypiPackageListIndexUpdateTaskTest
{
    @Test
    public void shouldParseLongDocument() throws Exception
    {
//        PypiPackageListIndexUpdateTask pypiPackageListIndexUpdateTask =
//                new PypiPackageListIndexUpdateTask(null, null, null, null, null);
//
//        InputStream fileStream = getClass().getResourceAsStream("packagesList.html");
//        List<Document> packageNames = pypiPackageListIndexUpdateTask.parseHtmlPageToPackagenames(fileStream,
//                pypiExtensionRepository);
//        assertEquals(113032, packageNames.size());
    }
}